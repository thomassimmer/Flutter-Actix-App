import 'package:flutter/material.dart';

class AppColors {
  // Primary and secondary colors
  static const Color primary = Color.fromRGBO(144, 202, 249, 1);
  static const Color secondary = Color.fromRGBO(35, 123, 255, 1);

  // Complementary colors for the theme
  static const Color primaryVariant = Color.fromRGBO(13, 71, 161, 1);
  static const Color secondaryVariant = Color.fromRGBO(185, 211, 253, 1);

  // Neutral colors
  static const Color background = Color.fromRGBO(255, 255, 255, 1);
  static const Color surface = Color.fromRGBO(255, 255, 255, 1);
  static const Color error = Color.fromRGBO(255, 176, 0, 1);

  // Text and icon colors
  static const Color onPrimary = Color.fromRGBO(255, 255, 255, 1);
  static const Color onSecondary = Color.fromRGBO(0, 0, 0, 1);
  static const Color onBackground = Color.fromRGBO(0, 0, 0, 1);
  static const Color onSurface = Color.fromRGBO(0, 0, 0, 1);
  static const Color onError = Color.fromRGBO(255, 255, 255, 1);
}
